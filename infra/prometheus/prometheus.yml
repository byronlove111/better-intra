global:
  scrape_interval: 15s

# need to monitor: cadvisor / postgres-exporter / nginx-exporter / backend
scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
