global:
  scrape_interval: 15s

# reste a faire: postgres-exporter / nginx-exporter / backend (placeholder,
# attend le /metrics FastAPI — cf. SUGGESTIONS_MALIK.md et
# PLAN_PROMETHEUS_GRAFANA.md dans ft_transcendenc)
scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: cadvisor
    static_configs:
      - targets: ["cadvisor:8080"]
